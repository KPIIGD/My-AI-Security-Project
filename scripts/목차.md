# scripts/ — 유틸리티 스크립트 목차

> 이 폴더는 루트 레벨 **유틸리티 스크립트**를 보관합니다.
> 제품 평가 스크립트는 [`../korean_pii_guardrail_v0_2/scripts/`](../korean_pii_guardrail_v0_2/목차.md)에 있습니다.
> 루트 목차: [`../목차.md`](../목차.md)

---

## 파일 목록

| 파일 | 역할 | 런타임 |
|------|------|--------|
| `build_pptx_v5.py` | 발표 PPT v5 자동 생성 스크립트 | Python |
| `run_eval_pipeline.sh` | 전체 평가 파이프라인 실행 셸 스크립트 | Bash |
| `fetch_slack_logs.py` | 팀 슬랙 대화를 날짜별 md로 수집 (→ `../슬랙/`) | Python |

---

## 스크립트 상세

### `build_pptx_v5.py`

**역할:** `python-pptx` 기반으로 발표 슬라이드 v5를 프로그래밍 방식으로 생성  
**출력:** `presentations/AI_Gateway_가드레일_프로젝트_발표_v5.pptx`

```bash
python scripts/build_pptx_v5.py
```

### `run_eval_pipeline.sh`

**역할:** `PII/evaluation/` 의 평가 스크립트들을 순서대로 실행하는 자동화 셸  
**실행 순서:** sample → baseline → L0 compare → 4-way compare → final

```bash
bash scripts/run_eval_pipeline.sh
```

### `fetch_slack_logs.py`

**역할:** Slack Web API로 채널 대화를 조회해 `../슬랙/<날짜>.md` 로 저장 (의존성 없는 stdlib만 사용)  
**특징:** **멱등** — 같은 날짜를 반복 실행해도 중복 없이 갱신하고 `## 📌 요약` 블록은 보존. 스케줄러로 N분마다 돌려 **사실상 실시간 수집** 가능. 채널 미지정 시 접근 가능한 공개 채널 자동 탐지.  
**입력:** 환경변수 `SLACK_BOT_TOKEN` (scope: `channels:history`, `channels:read`, `users:read`)  
**출력:** `슬랙/<날짜>.md` (`.gitignore` 처리되어 공개 레포에 커밋되지 않음)

```bash
# PowerShell:  $env:SLACK_BOT_TOKEN = "xoxp-..."
export SLACK_BOT_TOKEN=xoxp-...
python scripts/fetch_slack_logs.py                 # 오늘, 공개 채널 전체 자동
python scripts/fetch_slack_logs.py --days 2        # 오늘+어제 (스케줄러 권장)
python scripts/fetch_slack_logs.py --channel general,random --date 2026-06-18
```

---

_최종 업데이트: 2026-06-18_
